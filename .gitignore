# Rust
/target
**/*.rs.bk

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local config / secrets
*.local.toml
.env
.env.*
!.env.example

# Local agent-tool state (Claude Code, Codex, etc.)
.claude/
.codex

# Ad-hoc pcaps captured during debugging
/*.pcap

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Test artifacts
/test-harness/**/*.log
/test-harness/**/*.pcap
/test-harness/**/*.wav

# SIPp run artifacts — error traces (`*_errors.log`) and temp scenarios
# land in the current working directory, which is often the repo root.
*.log
/tmp.*

# Docker
docker/.data/
