[package]
name        = "siphon-ai-admin-api-types"
description = "Admin API wire types shared by the daemon (serializer) and admin clients such as sightglass (deserializer)."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
repository.workspace   = true

[dependencies]
serde.workspace = true
# For the recent-CDRs passthrough envelope only — CDR entries are the
# serialized siphon-ai-cdr records (the CDR schema is the schema; this
# crate doesn't re-model it).
serde_json.workspace = true
