# Registering SiphonAI as a Phone

**Status:** stub. See `DEV_PLAN.md` §7.2.

SiphonAI can register to a PBX (Asterisk, FreePBX, CUCM) as a SIP endpoint
("registered phone" mode), so calls dialed to its extension are bridged to a
WebSocket server. The alternative is trunk mode (UAS).
