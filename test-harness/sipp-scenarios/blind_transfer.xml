<?xml version="1.0" encoding="UTF-8"?>
<!--
  blind_transfer — SIPp acts as the caller; SiphonAI is the UAS. After
  the call is established the WS server sends `BridgeIn::Transfer` (an
  external harness drives this — see run-all.sh notes), which makes
  SiphonAI issue an in-dialog REFER. SIPp validates that:

    * REFER arrives inside the established dialog
    * SiphonAI handles the 202 Accepted correctly
    * SiphonAI tears down with BYE (StopReason::Transfer)
    * the refer-progress NOTIFY sent after the BYE (RFC 3515 implicit
      subscription — Twilio does this) is answered 200 OK, not 405
      (#357). Deliberately sent post-BYE: that is when a real peer's
      NOTIFY lands, after SiphonAI's dialog is already gone.

  This scenario REQUIRES a WS test server that emits Transfer; the
  bare daemon with the echo example won't trigger it. The companion
  WS helper lives in test-harness/ws-transfer-helper/ (TBD).
-->
<scenario name="blind_transfer">
  <send>
<![CDATA[
INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
Max-Forwards: 70
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:sipp@[local_ip]:[local_port]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=user1 53655765 2353687637 IN IP4 [local_ip]
s=-
c=IN IP4 [local_ip]
t=0 0
m=audio 9000 RTP/AVP 0
a=rtpmap:0 PCMU/8000
]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true"/>

  <send>
<![CDATA[
ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]-ack
Max-Forwards: 70
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Content-Length: 0

]]>
  </send>

  <!-- Wait for the WS server to emit BridgeIn::Transfer, which
       causes SiphonAI to send REFER on this dialog. Generous
       timeout so a slow CI box doesn't false-fail. -->
  <recv request="REFER" timeout="5000"/>

  <send>
<![CDATA[
SIP/2.0 202 Accepted
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:sipp@[local_ip]:[local_port]>
Content-Length: 0

]]>
  </send>

  <!-- After REFER, SiphonAI sends Stop on the WS and tears down,
       which on the SIP side appears as BYE on this dialog. -->
  <recv request="BYE" timeout="5000"/>

  <send>
<![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

  <!-- Refer-progress NOTIFY on the (now torn-down) dialog, exactly as
       a subscription-tracking trunk sends it. Must draw 200 OK, not
       405 Method Not Allowed (#357). -->
  <send>
<![CDATA[
NOTIFY sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]-notify
Max-Forwards: 70
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 NOTIFY
Contact: <sip:sipp@[local_ip]:[local_port]>
Event: refer
Subscription-State: terminated;reason=noresource
Content-Type: message/sipfrag;version=2.0
Content-Length: [len]

SIP/2.0 200 OK
]]>
  </send>

  <recv response="200" timeout="5000"/>
</scenario>
