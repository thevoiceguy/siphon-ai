<?xml version="1.0" encoding="UTF-8"?>
<!--
  unsupported_codec_488 — INVITE with an audio-only offer that lists
  ONLY a codec SiphonAI doesn't speak (G.722 / PT 9, when defaults
  are pcmu+pcma). The SDP negotiation step must fail with
  "no common codec" and the acceptor must return 488 Not Acceptable
  Here (see crates/core/src/acceptor.rs failure mapping).

  ACK and dialog termination are SIPp's responsibility once a final
  non-2xx response lands — no further BYE needed.
-->
<scenario name="unsupported_codec_488">
  <send>
<![CDATA[
INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
Max-Forwards: 70
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:sipp@[local_ip]:[local_port]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=user1 53655765 2353687637 IN IP4 [local_ip]
s=-
c=IN IP4 [local_ip]
t=0 0
m=audio 9000 RTP/AVP 9
a=rtpmap:9 G722/8000
]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="488" rtd="true"/>

  <send>
<![CDATA[
ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
Max-Forwards: 70
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Content-Length: 0

]]>
  </send>
</scenario>
